# Livelist environment variables (example)

# The app's domains — used for subdomain detection and shared auth cookies.
# Separate more values with the ':'
LIVELIST_DOMAINS=localhost
LIVELIST_SHEET_STORE_PATH="/srv/sheets/"
