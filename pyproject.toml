[project]
name = "livelist"
version = "1.1.0"
dependencies = [
    "sqlalchemy",
    "flask",
    "flask_socketio",
    "flask_sqlalchemy",
    "pyyaml",
]

[tool.setuptools]
packages = [
    "livelist",
]
